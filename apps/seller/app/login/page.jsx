"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function Login(){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),r=await fetch("/api/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})}),d=await r.json();setBusy(false);if(!r.ok)return setError(d.error);router.push("/");router.refresh()}return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-[#061d2b] to-[#087f8c] p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><p className="text-xs font-bold uppercase tracking-widest text-[#087f8c]">MySME Seller</p><h1 className="mt-2 text-3xl font-black">Seller sign in</h1><p className="mt-2 text-sm text-slate-500">Manage products, leads and your company.</p><input name="email" type="email" required placeholder="Business email" className="mt-7 h-12 w-full rounded-xl border px-4"/><input name="password" type="password" required placeholder="Password" className="mt-3 h-12 w-full rounded-xl border px-4"/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={busy} className="mt-5 h-12 w-full rounded-xl bg-[#087f8c] font-bold text-white">{busy?"Signing in...":"Sign in"}</button></form></main>}
